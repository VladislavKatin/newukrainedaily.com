name: Release Check

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  release-check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Run release check
        run: npm run release:check

      - name: Telegram failure
        if: failure() && github.event_name != 'pull_request' && secrets.CRON_SECRET != ''
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          curl --fail-with-body --show-error --silent \
            -X POST "https://www.newukrainedaily.com/api/internal/telegram" \
            -H "Authorization: Bearer $CRON_SECRET" \
            -H "Content-Type: application/json" \
            --data '{"status":"error","title":"Сбой release-check","text":"Не прошли проверки релиза. Проверьте lint, build, smoke и visual тесты в GitHub Actions."}'
